
  # Generate clickable web UI

  This is a code bundle for Generate clickable web UI. The original project is available at https://www.figma.com/design/YSxx3TvtqareBo1UipJFHK/Generate-clickable-web-UI.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  